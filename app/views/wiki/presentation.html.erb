<!doctype html>
<html lang="en">

  <head>
    <meta charset="utf-8">

    <title>Presentation</title>

    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <%= stylesheet_link_tag "reveal.min.css", :plugin => "redmine_presentation" %>
    <%= stylesheet_link_tag "theme", :plugin => "redmine_presentation" %>
    <%= stylesheet_link_tag "fix", :plugin => "redmine_presentation" %>

		<!-- If the query includes 'print-pdf', include the PDF print sheet -->
    <!--
		<script>
			if( window.location.search.match( /print-pdf/gi ) ) {
				var link = document.createElement( 'link' );
				link.rel = 'stylesheet';
				link.type = 'text/css';
				link.href = 'css/print/pdf.css';
				document.getElementsByTagName( 'head' )[0].appendChild( link );
			}
		</script>
    -->
	</head>
	<body>
    <div class="reveal">
      <div class="slides">
          <%= textilizable @content, :text, :attachments => @content.page.attachments  %>
      </div>
    </div>

    <%= javascript_include_tag "head.min.js", :plugin => "redmine_presentation" %>
    <%= javascript_include_tag "reveal.min.js", :plugin => "redmine_presentation" %>
    
		<script>

			// Full list of configuration options available here:
			// https://github.com/hakimel/reveal.js#configuration
			Reveal.initialize({
				controls: false,
				progress: true,
				history: true,
				center: true,
				slideNumber: true,
				mouseWheel: true,

				theme: Reveal.getQueryHash().theme, // available themes are in /css/theme
				transition: Reveal.getQueryHash().transition || 'default', // default/cube/page/concave/zoom/linear/fade/none

				// Optional libraries used to extend on reveal.js
				dependencies: []
        
			});

		</script>
    
  </body>
</html>
